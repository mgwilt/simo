---
type: Work Decision Log
title: Breeze performance decisions
description: Breeze performance decisions for bounded M3 Ultra implementation and evidence.
tags: [work, breeze, performance, mps]
status: draft
generated: { by: process:simo-performance-integration, at: 2026-09-05T16:51:00Z }
simo:
  profile_version: 1
  stable_id: W-20260904-breeze-mps-performance-DECISIONS
  authority: coordination
  repository_paths: [vendor/breeze-tts, services/breeze, python/simo, web, tests, docs]
  owner: process:simo-performance-integration
  work: { parent_id: W-20260904-breeze-mps-performance }
---
# Breeze performance decisions

- **D-018:** Latest user instruction authorizes running the existing fast MLX eight-bit candidate in full Simo for manual testing without completing acceptance first. This supersedes earlier restrictions on using it for conversation, not historical measurements. Select the existing experimental recipe/endpoint explicitly; do not pretend the reserved released-Fast CLI label or skipped physical/quality gates are accepted. Keep immutable identity profiles, Quality defaults and locked rollback unchanged. Only startup/connectivity checks are required for this launch.

- **D-001:** Dedicated Breeze-only project; former identities plan is read-only until a subsequent serialized mutation handoff. Keep completed historical artifacts and physical conversation acceptance there.
- **D-002:** Remove repeated computation and full-response buffering before quantization. Quality preserves CFG and sampling defaults; lower CFG is a separately labeled tradeoff.
- **D-003:** Keep Transformers 4.57.3/PyTorch 2.9.1 as reference. Qwen compatibility and exact Breeze generation APIs gate any migration; dependency overrides are not proof.
- **D-004:** Fast promotion requires A-006 and quality acceptance, not just a mode label or a newer backend. Preserve rejected candidates and rollback.
- **D-005:** Initially a whole MLX rewrite was deferred to a separately authorized follow-up. D-012 supersedes this scope restriction; failed gates still cannot be waived.

Promote accepted runtime decisions into the existing Breeze interface and LAN operations concepts.

- **D-006:** Native torch2.9.1 MPS kernels are the isolated quantization experiment; no new dependency/kernel package or checkpoint overwrite. Both bit widths remain candidates because complete-utterance throughput failed.
- **D-007:** Compiled static depth remains experimental: real BF16 logit choices differ on the bounded reference comparison. SDPA remains explicitly selectable, not an automatically promoted Fast recipe. Quality's eager attention and requested CFG remain default.
- **D-008:** The user prefers CLI/scripts and no computer use. Retain physical playback/listening gates as unverified, not replaced by HTTP or worklet timestamps.
- **D-009:** Follow-up is bounded paired-backbone-cache feasibility (different prompt lengths, branch positions, padding, reuse/cancellation), then MLX feasibility if needed. No full backbone cache-ownership rewrite or MLX port is hidden in this change.
- **D-010:** Fork commits support the submodule pin. On2026-09-05 the user explicitly authorized commit and publication of both repositories; the MLX implementation was published at `05129be2`, followed by the fork README and final parent pin at `78a79bbe7996f88766ee1885140909ca696c7055`, before the parent gitlink. Earlier no-publication checkpoints remain historically accurate.
- **D-019:** T-023 publication is a resumable implementation checkpoint, not Fast release promotion or Work Plan closure. Publish the current scoped implementation, tests, documentation and exact fork gitlink to each `origin`; leave the unrelated untracked `:memory:.ses` untouched. Record initially sandbox-denied Metal and the host-access200-test rerun rather than weakening the gate. The known LAN server-lifetime coupling remains an open follow-up rather than being silently fixed as part of publication.
- **D-011:** The user reports choppy streaming and requests more buffering. Preview buttons now favor continuous speech over early partial audio: prebuffer the complete clip, bounded to the existing 120-second server limit, before playback. A small lead cannot sustain playback with measured RTF above one. Network/codec streaming and Stop remain; live conversations and model settings are unchanged. This supersedes D-002's browser early-play requirement for previews only and leaves A-006 failed. Promote the tradeoff and limits into LAN operations.
- **D-012:** The user explicitly authorizes model, inference and training enhancements, including an MLX rewrite if other approaches cannot meet the goal. Escalate from measured bottlenecks through bounded experiments into implementation when justified; another scope approval for MLX is not required. This supersedes the full-rewrite deferral in D-005/D-009 and earlier checkpoint wording. Preserve reference weights/environment, lawful training inputs, single-owner mutation leases and all quality/performance gates. No paid compute, data sharing or publication is implied.
- **D-013:** E-008 establishes a promising isolated MLX depth candidate, not a Fast recipe. Prefer eight-bit over four-bit for further evaluation: about 33 versus 30 ms depth p95, with materially less numerical drift. Keep BF16 controls and localize backend arithmetic differences before quality conclusions. Remaining backbone/codec work justifies the next bounded MLX extension under D-012. No serving/default change, model export or dependency-lock migration follows from this microbenchmark.
- **D-014:** E-011 meets the fixed local producer throughput screen before/with idle resident models and proves bounded actual cleanup/retry. Under D-012, the next bounded implementation is a separately named/versioned experimental mlx-int8-v1 service and preview-only site, not accepted Quality or Fast. Retain Quality defaults and reject unaccepted Fast; preserve current ports/assets/conversation services. Use an isolated existing MLX overlay, loopback inference and separate local-network preview port with current trusted TLS. No identity, STT/LLM, lock, trust-store or publication changes. Reject unsupported request behavior rather than silently changing CFG/seed/reference inputs. Progressive playout and its late-failure exception require a later explicit scoped decision and tests; this decision alone changes no runtime.
- **D-015:** T-013 uses direct Uvicorn HTTPS with the existing trusted certificate on a separately owned LAN socket, instead of another Caddy/LiveKit supervisor. The preview-only factory serves allowlisted built assets and three fixed instruction previews, rejects cross-origin/unapproved Host requests, and creates no alias, conversation, persistence, room token or STT/LLM runtime. Preserve the original Quality process and web/dist. Explicit experimental identity and complete-clip playback remain; verified TLS/PCM is not audible acceptance. The corrected CLI proof must match every PCM/WAV response fingerprint to the sidecar, including the cancellation trial, and reject runtime changes.
- **D-016:** The faster measured MLX producer justifies a bounded progressive-preview experiment, not Fast promotion. Only an operator-selected exact runtime fingerprint on the isolated preview site may advertise mlx-stream-v1; Quality/default/unknown runtimes retain complete-clip buffering. Require response fingerprint/policy agreement before posting PCM, two-second posted-minus-consumed credits, a measured480/640ms startup/rebuffer reserve, and the existing120-second total limit. This narrowly supersedes D-011's silent-until-complete guarantee for the opted-in experimental route: a later response/codec failure may follow already heard audio, but must abort/clear queued audio, preserve the error, never claim completion/cache partial output, and permit Stop/retry. Tests and full observed-arrival replays precede serving selection. Scheduling is not audible proof; all A-006/A-007 gates remain.
- **D-017:** Add a startup-only benchmark opt-in to the isolated exact-runtime streaming site. Only server-owned fixed short/long corpus indices and default/three existing instruction IDs with strict uint32 seeds are accepted; no arbitrary text/reference fields or preview-cache writes. Every run binds a hashed corpus/source/build manifest, runtime fingerprint and actual sidecar request ID. Expose only the matching completed last-request metrics; reject stale/mismatched/incomplete results. Keep the normal client loopback HTTP allowlist unchanged. Measure unpaced HTTPS arrival/producer throughput separately from replay/consumer pacing, retain exact warmup triplets and all failures, and repeat matched suites with idle STT/LLM weights. Same-host TLS/replay is not physical device/listening acceptance; no Fast promotion follows automatically.
