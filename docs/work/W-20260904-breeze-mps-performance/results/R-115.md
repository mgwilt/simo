---
type: Result Packet
title: Uncached HTTPS benchmark boundary recommendation
description: Read-only next-stage corpus and request-identity design separates producer throughput from paced playback without adding runtime routes.
tags: [work, breeze, performance, playback]
status: draft
generated: { by: process:simo-performance-integration, at: 2026-09-05T08:35:28Z }
simo:
  profile_version: 1
  stable_id: W-20260904-breeze-mps-performance-R-115
  authority: coordination
  repository_paths: [python/simo, web, tests/python, scripts]
  owner: process:simo-performance-integration
  work: { parent_id: W-20260904-breeze-mps-performance }
---
# Uncached HTTPS benchmark boundary recommendation

[T-115](../tasks/T-115.md) complete read-only by fast_review. Inspected current benchmark, preview service, transport, holder, tests and E-011/E-012. No changes, model/service runs, installs or computer use. This is next-stage advice; no benchmark endpoint or CLI extension is implemented by this result.

Existing fixed-three-preset previews are cached and not the ten-prompt/three-seed release suite. Recommend an operator-enabled experimental-only POST `/api/benchmarks/{short|long}/{index}/stream` with server-owned corpus/instruction IDs, bounded uint32 seed, exact expected runtime and manifest hash. No arbitrary text/reference input or cache writes. Return truthful BYPASS and request identity, keeping normal preview/cache routes unchanged. An optional `--url/--ca-file/--connect-address` on the existing CLI can measure that route while retaining verified TLS. Do not weaken BreezeHTTPSynthesizer's loopback HTTP allowlist to support it.

Bind response and subsequent service metrics through propagated X-Breeze-Request-ID as well as the runtime fingerprint; current benchmark health.last_request is not request-ID-bound. Record exact warmup triplets, all corpus/settings/source/build/policy identities, first PCM/EOF/arrivals, completed PCM hashes and failures. Align warmup schedules explicitly: current producer and loopback probes do not use the same sequence.

Run unpaced HTTPS throughput separately from credit-paced playback: a two-second playback queue can pace network arrival RTF toward one even when the producer meets0.8. Do not score that consumer-paced RTF as producer failure or conflate a retained trace replay with live network backpressure. T-014's actual player/worklet harness supplies logic replay, not real browser/device timing. Same-host LAN-IP/TLS is not another device's Wi-Fi.

Proposed bounded T-015 paths: python/simo/{preview_site,breeze,cli,inference}.py; corresponding focused tests; scripts/hold_breeze_resident_models.py only if holder metadata needs extension; this Work Plan/operational contract. Fork request-ID propagation needs its own exact inclusion if absent. Run three explicit warmups, ten fixed prompts×17/29/42 plus two long prompts×three seeds, then repeat with the existing idle-model holder and ready/status/stopped timestamps. Preserve Quality and the selected experimental runtime; no hidden cache deletion or invented fingerprint. Root must inspect source, select the precise interface and acquire the next sole-writer lease before implementation.

Final read-only evidence consistency follow-up: reviewer independently verifies E-013's replay/served report hashes, all six input hashes,75 exact completed clips, cohort timings, zero simulated gaps,48,000-frame bounds, all ten held-source hashes and all ten candidate/Quality served-asset hashes. Three actual previews are HIT with unchanged last model request and runtime identities. No finding; simulated/physical and cache/uncached boundaries remain explicit. Documentation106 concepts/zero errors/two existing warnings, five knowledge tests and scoped diff checks pass. No source changes, model runs or service actions.
