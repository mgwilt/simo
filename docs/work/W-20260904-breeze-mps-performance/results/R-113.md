---
type: Result Packet
title: Isolated preview site review result
description: Independent held-source ASGI, browser-build isolation and HTTPS lifecycle review without physical playback acceptance.
tags: [work, breeze, preview, streaming]
status: draft
generated: { by: process:simo-performance-integration, at: 2026-09-05T07:53:18Z }
simo:
  profile_version: 1
  stable_id: W-20260904-breeze-mps-performance-R-113
  authority: coordination
  repository_paths: [python/simo, web, tests/python]
  owner: process:simo-performance-integration
  work: { parent_id: W-20260904-breeze-mps-performance }
---
# Isolated preview site review result

[T-113](../tasks/T-113.md) complete read-only by buffer_review. Held preview/lan/CLI/build source hashes are [E-012](../evidence/E-012-experimental-serving.md). No edits, GPU/model execution, service actions or UI automation. Independently ran12 focused Python tests,25 web tests and TypeScript checking; later reran four preview tests after the annotation-only change.

No blocking finding. The preview-only factory exposes fixed previews and allowlisted built assets, rejects Host/cross-origin requests, and creates no alias/session/store/LiveKit/STT/LLM runtime. Unknown POST paths may return405 instead of404; neither exposes an API. Built assets contain no LiveKit/RTCPeerConnection/getUserMedia/session/start-conversation client. Conversation modules are transitively imported but not instantiated. Root verifies five original UI asset hashes remain unchanged.

Direct HTTPS owns the exact socket before startup, loads existing TLS configuration, waits for its own server.started flag, and uses should_exit/task completion/socket cleanup on cancellation. The five-second graceful timeout bounds request draining, not every possible startup/shutdown failure; no concrete hang path was found with the current default lifespan. Root subsequently proves actual certificate-verified TLS, Ctrl-C listener release and restart in E-012; the reviewer did not execute those external checks.

The sole post-review preview-source change is an explanatory comment plus scoped ty invalid-argument-type suppression at add_middleware. Removing it reproduces the prior reviewed SHA exactly. Installed Starlette confirms the documented factory/ASGI alias typing mismatch; BasedPyright and actual ASGI tests pass. No runtime behavior changed.

No Fast, progressive-playback or physical listening acceptance. The separate page keeps complete-clip buffering. Next T-014 requires a scoped policy and full arrival-replay/transport/resident tests; existing Quality policy stays intact.
