---
type: Result Packet
title: MLX backbone implementation boundary
description: Verified Qwen3 backbone weights, conditioning, cache positions and EOS rules for the next isolated performance experiment.
tags: [work, breeze, mlx, inference]
status: draft
generated: { by: process:simo-performance-integration, at: 2026-09-05T05:26:44Z }
simo:
  profile_version: 1
  stable_id: W-20260904-breeze-mps-performance-R-107
  authority: coordination
  repository_paths: [vendor/breeze-tts, services/breeze, docs/work/W-20260904-breeze-mps-performance]
  owner: process:simo-performance-integration
  work: { parent_id: W-20260904-breeze-mps-performance }
---
# MLX backbone implementation boundary

T-107 complete read-only by mlx_mapping. Exact source, locked Transformers 4.57.3 and safetensors-header inspection; no reviewer files, exports, GPU work, services or commits. Inputs and authorization: [T-107](../tasks/T-107.md). This map advances T-010 planning, not runtime or A-003/A-006 acceptance.

## Architecture and weights

Critical: models/breeze_backbone_factory.py BreezeBackboneAdapter constructs **Qwen3** layers using nested backbone_config, not BreezeDecoderLayer/top-level BreezeConfig. Effective values: 28 layers, hidden 2048, intermediate 6144, Q16/KV8, head128, GQA2, RMS epsilon 1e-6, RoPE theta 1e6 with no scaling, no biases/sliding window. Learned Q/K per-head RMSNorm occurs before RoPE. Do not reuse depth's epsilon 1e-5 or scaled rotary configuration.

| Suffix under backbone_model.layers.0..27 | Shape |
|---|---|
| self_attn.q_proj/o_proj.weight |[2048,2048]|
| self_attn.k_proj/v_proj.weight |[1024,2048]|
| self_attn.q_norm/k_norm.weight |[128]|
| mlp.gate_proj/up_proj.weight |[6144,2048]|
| mlp.down_proj.weight |[2048,6144]|
| input/post_attention_layernorm.weight |[2048]|

Final backbone_model.norm.weight is [2048]. All 309 serialized backbone tensors are BF16, totaling 2,818,820,096 bytes excluding shared audio embedding/custom head. Reuse depth primitives selectively, not its complete layer/config/cache class.

## Conditioning and caches

Initially retain Torch's locked T5Gemma2 text path. model._merge_input_ids_with_input_values produces full BF16 inputs_embeds[B,L,2048], preserving text/reference segments and instructions. text_encoder_proj.weight is [2048,1152]; no DimFusion/layer-projection tensors. Plain embed_text_tokens lookup would bypass actual conditioning.

For one generated codec frame, sum the 16 shared embedding rows at c[i]+i*2051 into one 2048-dimensional input. The [32816,2048] table is serialized only as depth_decoder.model.embed_tokens.weight; preserve tying and test sum precision. No audio projector. Normalized backbone hidden feeds both lm_head.weight [2052,2048] and depth conditioning.

Current separate CFG caches derive logical positions from cumsum(attention_mask)-1, masked positions set to1. A paired representation may left-pad prefixes/caches to common physical storage while preserving each row's padding mask and logical RoPE position. For lengths Lc/Lu, first frame uses logical Lc/Lu even at physical max(Lc,Lu). Each paired layer cache is [2,8,P,128]. Backbone cache persists across frames and resets per utterance; depth cache resets per frame. Right padding requires explicit last-valid selection and is not interchangeable.

## Generation and lifecycle

Critical: backbone EOS is 2051, with only 2048–2050 reserved. Depth's mask would suppress backbone EOS. models/generation_breeze.py applies F32 CFG, configured HF processors/warpers, then reserved masking, then softmax/multinomial or greedy. HF top-k retains all ties at kth threshold; depth selects exactly k. HF top-p has separate ascending cumulative semantics. Do not reuse depth filtering blindly.

Portable serving actually uses repetition 1.0 from effective model configuration. FastStreamingConfig's 1.1 constant is not applied by portable runtime. Preserve the actual processor list; any later penalty must apply to generated codebook0 history, not all prompt/other-codebook tokens. EOS skips depth/codec and current streaming represents termination with all2050 pads. Codec token0 is valid, not utterance EOS. Keep current input limits and750-frame ceiling despite the backbone's larger positional limit.

Next smallest experiment: preserve Torch text preparation/prefill; bridge branch final hidden and K/V once, then compare paired MLX continuation on fixed real codec frames. Only afterward implement paired MLX prefill and the complete codebook0/depth loop with one codec bridge per frame. Test unequal prefixes/adversarial padding, per-head Q/K norms, early/long positions, branch swapping/reuse, audio sums,2052-row head, EOS/top-k ties/reserved masks, max length, cancellation and clean retry. Measure bridge/decode/head/CFG/depth together; preserve full utterance, resident, quality and physical-playout gates. [Checkpoint](../checkpoint.md).

## Continuation review and next boundary

T-010 implementation and production screens are now recorded in [E-009](../evidence/E-009-mlx-backbone.md). Root fixed reviewer findings: reject mixed head/embedding precision, unsupported audio projector, nonbinary transferred masks and future-valid slots; prove full prior-state immutability and final partial cache expansion. Tests22 backbone/113 total pass. fast_review independently verified held source501715ef, same-source BF16/8-bit reports and quantization coverage. Reference cache reconstruction was removed from timing; differing compiled/uncompiled numerical results are explicitly retained. No reviewer mutations or GPU work.

T-011 mapping follow-up: preserve actual portable settings, not unused fast constants. Both stages temperature0.9/top-k50/top-p1/sample true, repetition1.0 and750 iterations. Keep one explicit MLX key across c0 and15 depth samples. Backbone top-p uses ascending cumulative probability≤1−p with at least one retained token; depth filtering differs. Reject unsupported custom processors/modes initially. EOS2051 skips depth/codec, no-EOS at the limit is truncation, immediate EOS is empty-output failure. Reuse the existing portable queue/codec lifecycle; check cancellation around prefill/stages/emission. Measure request timing before preparation/prefill, bridges and codec included. Actual full audio/ASR, then long/resident/listening and browser buffering gates remain. Initial full-utterance experiments may retain Torch prefill; a separate MLX prefill rewrite is not a prerequisite for that bounded test.
