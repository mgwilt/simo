---
type: Result Packet
title: Matched MLX probe and ASR contract
description: Existing probe supports BF16/int8 screening while ordinal joins and PCM conversion limits prevent false comparison claims.
tags: [work, breeze, mlx, quality]
status: draft
generated: { by: process:simo-performance-integration, at: 2026-09-05T10:42:15Z }
simo:
  profile_version: 1
  stable_id: W-20260904-breeze-mps-performance-R-122
  authority: coordination
  repository_paths: [vendor/breeze-tts/breeze_infer/probe_mlx_speech.py, scripts/evaluate_breeze_audio.py, docs/work/W-20260904-breeze-mps-performance]
  owner: process:simo-performance-integration
  work: { parent_id: W-20260904-breeze-mps-performance }
---
# Matched MLX probe and ASR contract

[T-122](../tasks/T-122.md) complete read-only by fast_review. No files changed, model/GPU/network/service/audio execution or acceptance advancement. Root's next experiment needs a new exact lease; this record is not execution evidence.

The existing probe supports six rows with exactly text/instruction. Stable order: bright-guide zero-based5/6/9, grounded-mentor5, warm-companion5/9, copied verbatim from E-016's bound manifest. Seed crossproduct17/29/42 yields18 timed outputs per arm; three warmups repeat corpus0/seed17. Omit --limit (its small default-corpus bounds do not select six custom rows), --uncompiled and lifecycle checks. Compilation is default; omit --quant-bits for BF16, add --quant-bits8 for int8. Both arms quantize backbone/depth jointly, not individual layers.

Existing command, with separate new directories and captured stdout/stderr/exit status for each serial arm:

```sh
PYTHONPATH=vendor/breeze-tts UV_CACHE_DIR=/private/tmp/simo-uv-cache HF_HUB_OFFLINE=1 uv run --offline --project services/breeze --frozen --with mlx==0.32.0 --with pytest==8.4.2 python -m breeze_infer.probe_mlx_speech --model-path .models/Breeze-TTS-2 --corpus CORPUS_JSON --warmups 3 --seeds 17 29 42 --audio-dir NEW_BF16_DIRECTORY
```

Each arm must have exit0,18 samples/parallel audio_artifacts and three warmup result/artifact pairs (21 WAVs). There is no top-level completed flag: validate every sample.failure is null, producer completed/EOS/not-cancelled, exact schedule/full inputs and audio/arrival/codec totals. Schema1 probe reports are not schema3 HTTPS reports. Load/preparation/save failures can abort before final JSON; preserve the named attempt's stdout/stderr/exit and partial directory, never overwrite it. Iterator generation/codec exceptions retain partial WAVs and sample.failure/exit1. Independently audit before ASR, because the evaluator does not enforce these completion gates.

The existing `uv run --frozen python scripts/evaluate_breeze_audio.py BF16_REPORT INT8_REPORT` accepts probe samples/audio_artifacts directly. Its rows omit instruction/index; repeated text+seed across voices makes that an ambiguous join. Bind a hashed parent report plus ordinal, then validate that ordinal's full prompt/instruction/seed and WAV/PCM hash. Preserve all18 new int8 comparisons, including seven original flags and eleven clean cases; do not transfer old ASR silently.

Probe rounds clipped floats×32767 to PCM16; API truncates toward zero. First compare all18 new int8 files against their full matching E-015 HTTPS cases: equal codec/sample counts, maximum absolute PCM difference≤1 LSB, and nonzero differences pointing away from zero (rounded magnitude not below truncated magnitude). This establishes conversion compatibility, not byte identity or raw-float/codebook equivalence: the probe retains neither float audio nor codebook hashes. BF16 and int8 themselves share the rounded encoder, so no such tolerance workaround is needed between those arms.

Reports record source/corpus/kernel hashes, dependencies, effective settings and model marker, not fresh full weight hashes. Source/corpus/kernel hashes are read before model loading and marker at report end; hold/recheck source, corpus, marker, lock and model identity around both arms. CFG4, temperature0.9, top-k50, top-p1, max750, TorchBF16 preparation/prefill and FP32 codec remain unchanged. Different backend/quantized logits can amplify sampled choices; no cherry-picked seed/instruction, zero-WER vote or throughput result establishes listening acceptance.
