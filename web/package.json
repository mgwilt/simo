{
  "name": "simo-lan-voice",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:preview": "vite build --config vite.preview.config.ts",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "replay:preview": "node scripts/replay-preview.mjs"
  },
  "dependencies": {
    "livekit-client": "2.15.6"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vite": "7.1.3"
  }
}
