[project]
name = "simo-tools"
version = "0.1.0"
description = "Deterministic repository tooling for Simo"
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0,<7",
    "tiktoken>=0.13,<1",
]

[tool.uv]
package = false
